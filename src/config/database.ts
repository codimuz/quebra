import { DatabaseConfig } from '../services/database';

const databaseConfig: DatabaseConfig = {
  name: 'app-database.db',
  version: 1,
  description: 'Banco de dados principal do aplicativo'
};

export default databaseConfig;