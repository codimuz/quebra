import { MD3DarkTheme, MD3LightTheme } from 'react-native-paper';

// Tema escuro usando as cores padrão do React Native Paper
export const customDarkTheme = {
  ...MD3DarkTheme,
};

// Tema claro usando as cores padrão do React Native Paper
export const customLightTheme = {
  ...MD3LightTheme,
};
